export const REPOSITORY_RULE_UPD_1C = `# Правило для Юры: УПД в загрузку 1С

Если владелец прислал один или несколько файлов УПД (xlsx) и говорит примерно так: «переработай УПД для загрузки в 1С», «сделай в загрузку», — действуй строго по этому правилу. Ничего не переспрашивай сверх пункта 8.

## Главное
Каждый исходный файл даёт **один** результат. Файлы между собой **не объединять**. Дубли внутри файла **не схлопывать** — сколько строк в оригинале, столько и на выходе. Порядок позиций — **точно как в оригинале** (по колонке «№ п/п», она же 6-я).

## Что на выходе
Плоская таблица, один лист «Товары», 10 колонок строго в этом порядке:

1. Наименование товара
2. Артикул
3. Артикул (лат.)
4. Количество
5. Цена
6. Стоимость с налогом
7. Код страны
8. Страна
9. Рег. номер декларации
10. РРЦ

## Как читать исходник
Форма УПД — 74 колонки, с разрывами на листы: шапка таблицы и строка с буквенными номерами колонок («А», «1», «1а»…) повторяются на каждом листе, внизу идут подписи и реквизиты. Всё это — мусор, в результат не попадает.

**Товарная строка — та, где в колонке 6 лежит число** (порядковый номер). Это единственный надёжный признак: он сам отсекает и повторные шапки, и хвост с подписями.

Номера таможенных деклараций в колонке 69 хранятся формулами-ссылками на внешнюю книгу. Читать книгу нужно с \`data_only=True\`, иначе вместо номера придёт текст формулы вида \`=[1]TDSheet!BW20\`.

Карта колонок исходника:

| Колонка | Что в ней |
|---|---|
| 6 | № п/п (признак товарной строки) |
| 10 | Наименование |
| 27 | Количество |
| 59 | Стоимость с налогом — всего |
| 63 | Цифровой код страны |
| 67 | Краткое наименование страны |
| 69 | Рег. номер декларации |

## Как считать
- **Цена** = «стоимость с налогом» ÷ «количество», округление до 2 знаков. Брать колонку 30 (цену без налога) **нельзя** — в 1С нужна цена с налогом.
- **Стоимость с налогом** — из колонки 59 как есть, округление до 2 знаков.
- **РРЦ** = цена × 4, округление до 2 знаков.

## Артикул
Ищется в тексте наименования регулярным выражением \`[КK][ТT]-[0-9]+(?:-[0-9]+)?\` — обе буквы могут быть и кириллицей, и латиницей вперемешку (в исходниках встречается «KT-595» латиницей при общем русском названии). Дальше формируются две версии:

- **Артикул** — всегда кириллицей: «КТ-7166»
- **Артикул (лат.)** — всегда латиницей: «KT-7166»

Если у какой-то строки артикул не распознался — это сигнал, что регулярка не покрыла случай. Посчитать такие строки и сказать владельцу, а не молча оставить пустое поле.

## Проверка (обязательно)
Сумма колонки «Стоимость с налогом» должна совпасть с числом в имени исходного файла — оно и есть итог по документу («Копия 759915.xlsx» → 759915). Сверять по каждому файлу с точностью до копейки.

Не сошлось — **сказать владельцу прямо**, с указанием файла и величины расхождения. Не прятать, не округлять «на глаз», не выдавать за успех.

## Имя результата
\`В загрузку Китфорт <дата документа ДД.ММ.ГГ> <сумма>.xlsx\`

Например: «В загрузку Китфорт 10.09.26 759915.xlsx». Сумма — как в исходнике; если она с копейками, в имени файла на диске запятая заменяется на дефис (466322-50), а в названии для владельца остаётся запятой.

## Оформление
Шапка жирная, по центру, с переносом строк; первая строка закреплена; включён автофильтр по всей таблице. Формат чисел: количество — целое, цена / стоимость / РРЦ — \`#,##0.00\`. Ширины колонок: 52, 12, 14, 11, 12, 18, 11, 16, 26, 12.

Готовые файлы кладутся в \`public/converted/\` и регистрируются миграцией в таблице \`converted_files\` (title, description, file_url, file_name, s3_key, size_bytes) — после этого сами появляются в разделе «Конвертированные файлы».

## 8. Что уточнить у владельца
Только одно, и только если файлов несколько и дата в них разная: какую дату ставить в имена. Всё остальное берётся из этого правила.

В конце напомнить: чтобы скачать результат, сайт нужно опубликовать заново.`;
