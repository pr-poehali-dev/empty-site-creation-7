import { RECIPE_TELEGRAM_AUTH, RECIPE_TELEGRAM_AUTH_TITLE } from "./telegram-auth";
import { RECIPE_OWNER_DASHBOARD, RECIPE_OWNER_DASHBOARD_TITLE } from "./owner-dashboard";

export const REPOSITORY_RULE = `# Правило для Юры: Публикация рецепта (читать первым)

Если владелец говорит примерно так: «Юра, загляни в репозиторий, там правило — размести публично такой-то рецепт», действуй строго по этому правилу:

1. Найди запрошенный рецепт в списке Репозитория ниже (приватная часть внутри «Инструкции от Юры»).
2. Опубликуй его как простой текстовый файл \`.txt\` в папке \`public/recipes/<slug>.txt\`. Файлы из \`public\` отдаются по прямой ссылке как чистый текст — их без проблем читает другой ИИ/бот (в отличие от обычной страницы сайта: она SPA, и бот видит пустую оболочку).
3. Содержимое файла — голый текст рецепта: без markdown-разметки, без обратных кавычек. Блоки кода отделяй линиями из дефисов. Текст бери из общего источника \`src/data/repository\` (там тот же рецепт).
4. Прямую ссылку вида \`<адрес-сайта>/recipes/<slug>.txt\` НЕ публикуй нигде в коде, интерфейсе или текстах — пришли её только владельцу в чат.
5. Код в рецептах держи обезличенным: без реальных токенов, телефонов и ключей; секреты — только по именам.
6. По команде «убери рецепт из публичного доступа» — удали файл \`public/recipes/<slug>.txt\`. Приватная версия в Репозитории остаётся на месте.
7. Если рецепт ещё не создан — сначала уточни у владельца его состав, затем добавь в общий источник и в список Репозитория, и только потом публикуй.`;

export const REPOSITORY_RULE_CREATE = `# Правило для Юры: Создание рецепта

Если владелец говорит примерно так: «Юра, загляни в репозиторий, там правило „Создание рецепта", прочти его и создай такой-то рецепт», действуй строго по этому правилу:

1. **Сначала изучи реальный код** фичи в текущем проекте: фронтенд (страницы/компоненты), бэкенд (функция в /backend), таблицы БД и нужные секреты. Ничего не выдумывай — переноси то, что реально работает.
2. **Создай файл-источник** \`src/data/repository/<slug>.ts\` с двумя экспортами: \`RECIPE_<NAME>_TITLE\` (строка-заголовок) и \`RECIPE_<NAME>\` (markdown-константа в обратных кавычках).
3. **Зарегистрируй рецепт** в \`src/data/repository/index.ts\`: импортируй и добавь объект \`{ slug, title, content }\` в массив \`RECIPES\`. После этого рецепт сам появится в списке Репозитория.
4. **Обязательная структура рецепта** (в таком порядке):
   - «Привет, Юра!» — короткое вступление: что это и зачем.
   - «Что Юра должен спросить у владельца» — чек-лист вопросов перед внедрением.
   - «Как это устроено» — схема работы по шагам.
   - «Таблицы БД (SQL-миграция)» — если фича использует БД.
   - «Секреты» — только имена, без значений.
   - «Бэкенд» — код функций (index.py).
   - «Фронтенд» — код страниц/компонентов.
   - «Зависимости» — нужные npm-пакеты и ui-компоненты (shadcn/ui).
   - «Порядок внедрения» — финальный чек-лист по шагам.
   - Пометка о зависимости от других рецептов, если она есть.
5. **Код обезличивай**: URL функций и токены заменяй на плейсхолдеры вида \`<URL_ФУНКЦИИ_имя>\`; убирай реальные телефоны, ключи, имена ботов; секреты упоминай только по именам.
6. **Каждый блок кода** оформляй тройными обратными кавычками — на странице он рендерится с кнопкой «Скопировать».
7. **Ничего в рабочем проекте не ломай** — только добавляй файлы рецепта. Существующие страницы и логику не трогай.
8. Новый рецепт по умолчанию **только приватный** (в Репозитории). Публикуй его отдельно — по правилу «Публикация рецепта».`;

export interface Recipe {
  slug: string;
  title: string;
  content: string;
}

export const RECIPES: Recipe[] = [
  {
    slug: "telegram-auth",
    title: RECIPE_TELEGRAM_AUTH_TITLE,
    content: RECIPE_TELEGRAM_AUTH,
  },
  {
    slug: "owner-dashboard",
    title: RECIPE_OWNER_DASHBOARD_TITLE,
    content: RECIPE_OWNER_DASHBOARD,
  },
];

export const getRecipe = (slug: string): Recipe | undefined =>
  RECIPES.find((r) => r.slug === slug);