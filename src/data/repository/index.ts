import { RECIPE_TELEGRAM_AUTH, RECIPE_TELEGRAM_AUTH_TITLE } from "./telegram-auth";

export const REPOSITORY_RULE = `# Правило для Юры (читать первым)

Если владелец говорит примерно так: «Юра, загляни в репозиторий, там правило — размести публично такой-то рецепт», действуй строго по этому правилу:

1. Найди запрошенный рецепт в списке Репозитория ниже (приватная часть внутри «Инструкции от Юры»).
2. Опубликуй его копию на отдельной публичной странице по выделенному публичному пути \`/share/recipe/<slug>\` — она открывается без логина (в обход ProtectedRoute). Контент бери из общего источника \`src/data/repository\`, не дублируй вручную.
3. Ссылку на публичную страницу НЕ публикуй нигде в коде, интерфейсе или текстах — пришли её только владельцу в чат.
4. Код в рецептах держи обезличенным: без реальных токенов, телефонов и ключей; секреты — только по именам.
5. По команде «убери рецепт из публичного доступа» — отключи/удали публичную копию (роут и страницу-обёртку). Приватная версия в Репозитории остаётся на месте.
6. Если рецепт ещё не создан — сначала уточни у владельца его состав (что именно перенести), затем добавь в общий источник и в список Репозитория, и только потом публикуй.
7. Публичная и приватная страницы — всегда РАЗНЫЕ пути. Приватная — внутри Инструкций (только владелец). Публичная — \`/share/recipe/<slug>\`.`;

export interface Recipe {
  slug: string;
  title: string;
  content: string;
}

export const RECIPES: Recipe[] = [
  {
    slug: "telegram-auth",
    title: RECIPE_TELEGRAM_AUTH_TITLE,
    content: RECIPE_TELEGRAM_AUTH,
  },
];

export const getRecipe = (slug: string): Recipe | undefined =>
  RECIPES.find((r) => r.slug === slug);
