-- Товар попадает на склад двумя путями: решением мастера в приёмке и
-- перемещением между складами. Раньше писали только второе, поэтому
-- повторные заходы через приёмку исчезали из истории.
-- Признак источника отличает одно от другого: в истории пишем «(приёмка)».

ALTER TABLE receiving_moves
  ADD COLUMN IF NOT EXISTS source TEXT NOT NULL DEFAULT 'move';